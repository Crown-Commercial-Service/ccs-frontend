monolog:
    handlers:
        main:
            type: stream
            path: "%kernel.logs_dir%/symfony.log"
            level: error
            channels: ["!event"]
        rollbar:
            type: service
            id: app.rollbar_handler

