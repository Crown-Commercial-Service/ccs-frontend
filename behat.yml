---
default:
  extensions:
    Behat\MinkExtension:
      base_url: 'https://webuat.gca.gov.uk/'
      browser_name: 'firefox'
      # zombie session removed: behat/mink-zombie-driver has no Symfony 7 compatible
      # release. Pick a new driver (e.g. Selenium/Panther) before running bin/behat again.