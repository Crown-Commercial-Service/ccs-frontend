<div>
    <h2 class="govuk-heading-m" id="documents">Documents</h2>

    <p>You can use these documents to create a contract and buy from a supplier. The documents will help you to compliantly contract their services.</p>
    <p>You might not need all the documents to buy the products or services from this agreement.</p>
    <p>Each section below has the links to the documents, what they are for, when to use them and if you need to enter any information into them. </p>

</div>

<div class="govuk-accordion" data-module="govuk-accordion" id="accordion-default">
    {% if framework.content.customer_guide and framework.content.customer_guide != "" %}
        <div class="govuk-accordion__section ">
            <div class="govuk-accordion__section-header">
                <h2 class="govuk-accordion__section-heading">
                    <span class="govuk-accordion__section-button" id="accordion-default-heading-1">Customer Guide</span>
                </h2>
            </div>
            <div id="accordion-default-content-1" class="govuk-accordion__section-content" aria-labelledby="accordion-default-heading-1">
                {{ framework.content.customer_guide|raw }}
            </div>
        </div>
    {% endif %}

    {% if framework.content.core_terms_conditions and framework.content.core_terms_conditions != "" %}
        <div class="govuk-accordion__section ">
            <div class="govuk-accordion__section-header">
                <h2 class="govuk-accordion__section-heading">
                    <span class="govuk-accordion__section-button" id="accordion-default-heading-1">Core terms and conditions</span>
                </h2>
            </div>
            <div id="accordion-default-content-1" class="govuk-accordion__section-content" aria-labelledby="accordion-default-heading-1">
                {{ framework.content.core_terms_conditions|raw }}
            </div>
        </div>
    {% endif %}

    {% if framework.content.call_off_order_form and framework.content.call_off_order_form != "" %}
        <div class="govuk-accordion__section ">
            <div class="govuk-accordion__section-header">
                <h2 class="govuk-accordion__section-heading">
                    <span class="govuk-accordion__section-button" id="accordion-default-heading-1">Call-off order form</span>
                </h2>
            </div>
            <div id="accordion-default-content-1" class="govuk-accordion__section-content" aria-labelledby="accordion-default-heading-1">
                {{ framework.content.call_off_order_form|raw }}
            </div>
        </div>
    {% endif %}

    {% if framework.content.joint_schedules and framework.content.joint_schedules != "" %}
        <div class="govuk-accordion__section ">
            <div class="govuk-accordion__section-header">
                <h2 class="govuk-accordion__section-heading">
                    <span class="govuk-accordion__section-button" id="accordion-default-heading-1">Joint schedules</span>
                </h2>
            </div>
            <div id="accordion-default-content-1" class="govuk-accordion__section-content" aria-labelledby="accordion-default-heading-1">
                {% for jointSchedule in framework.content.joint_schedules %}
                    <p class="joint-schedule-title"><a href={{ jointSchedule.joint_schedule_document}}><strong>{{ jointSchedule.joint_schedule_document_name }}</strong></a></p>
                    <p class="joint-schedule-title">{{ jointSchedule.joint_schedule_document_type}}</p>
                    <p class="joint-schedule-title">{{ jointSchedule.joint_schedule_document_usage}}</p>
                    {{ jointSchedule.joint_schedule_document_description|raw }}
                    <br>
                {% endfor %}
            </div>
        </div>
    {% endif %}

    {% if framework.content.call_off_schedules and framework.content.call_off_schedules != "" %}
        <div class="govuk-accordion__section ">
            <div class="govuk-accordion__section-header">
                <h2 class="govuk-accordion__section-heading">
                    <span class="govuk-accordion__section-button" id="accordion-default-heading-1">Call-off schedules</span>
                </h2>
            </div>
            <div id="accordion-default-content-1" class="govuk-accordion__section-content" aria-labelledby="accordion-default-heading-1">
                
                <p>Call-off schedules are used when suppliers sell their goods or services to customers.</p>
                <p>You can choose which schedules to include and whether to edit these template schedules when they form a call-off contract.</p>
                
                {% for callOffSchedules in framework.content.call_off_schedules %}
                    <p class="joint-schedule-title"><a href={{ callOffSchedules.call_off_schedule_document}}><strong>{{ callOffSchedules.call_off_schedule_document_name }}</strong></a></p>
                    <p class="joint-schedule-title">{{ callOffSchedules.call_off_schedule_document_type}}</p>
                    <p class="joint-schedule-title">{{ callOffSchedules.call_off_schedule_document_usage}}</p>
                    {{ callOffSchedules.call_off_schedule_document_description|raw }}
                    <br>
                {% endfor %}
            </div>
        </div>
    {% endif %}

    {% if framework.content.framework_schedules and framework.content.framework_schedules != "" %}
        <div class="govuk-accordion__section ">
            <div class="govuk-accordion__section-header">
                <h2 class="govuk-accordion__section-heading">
                    <span class="govuk-accordion__section-button" id="accordion-default-heading-1">Framework schedules</span>
                </h2>
            </div>
            <div id="accordion-default-content-1" class="govuk-accordion__section-content" aria-labelledby="accordion-default-heading-1">
            
                <p>Framework schedules are used by CCS to set up the agreement with suppliers.</p>

                {% for frameworkSchedules in framework.content.framework_schedules %}
                    <p class="joint-schedule-title"><a href={{ frameworkSchedules.framework_schedule_document}}><strong>{{ frameworkSchedules.framework_schedule_document_name }}</strong></a></p>
                    <p class="joint-schedule-title">{{ frameworkSchedules.framework_schedule_document_usage}}</p>
                    {{ frameworkSchedules.framework_schedule_document_description|raw }}
                    <br>
                {% endfor %}
            </div>
        </div>
    {% endif %}

    {% if framework.content.templates and framework.content.templates != "" %}
        <div class="govuk-accordion__section ">
            <div class="govuk-accordion__section-header">
                <h2 class="govuk-accordion__section-heading">
                    <span class="govuk-accordion__section-button" id="accordion-default-heading-1">Templates</span>
                </h2>
            </div>
            <div id="accordion-default-content-1" class="govuk-accordion__section-content" aria-labelledby="accordion-default-heading-1">
                {{ framework.content.templates|raw }}
            </div>
        </div>
    {% endif %}

</div>