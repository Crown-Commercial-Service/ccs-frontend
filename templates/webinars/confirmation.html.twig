{% extends 'base.html.twig' %}

{% block title %}Access Webinar - {{ webinar.title|raw }} - CCS{% endblock %}

{% block header %}
    {% embed '/includes/header.html.twig' %}
        {% block bannerarea %}
        {% endblock %}
    {% endembed %}
{% endblock %}


{# Remove colour flourishes from this page #}
{% block flourishClasses %}{% endblock %}


{% block body %}

    <!-- required wrapper 1 -->
    <div class="govuk-width-container">

        <!-- If your design requires them, you should place components such as breadcrumbs, back link and phase banner inside this wrapper so that they sit directly underneath the header. -->


        <div class="govuk-phase-banner">
            {% include '/includes/phase-banner-content.html.twig' %}
        </div>

        <main class="govuk-main-wrapper">
            <div class="govuk-grid-row">
                <div class="govuk-grid-column-full">
                    <h1 class="govuk-heading-xl page-title">You can now watch the webinar below</h1>

                    {#<div class="page-intro"><p>You can now view the webinar below.</p></div>#}

                    {% if webinar.content.webinar_video is defined and webinar.content.webinar_video is not empty %}
                        <div class="responsive-video widescreen">{{ webinar.content.webinar_video|raw }}</div>
                    {% endif %}
                </div>
            </div>
        </main>

    </div>

    {% include 'forms/dataLayer_component/_form_submit.html.twig' with {'formName': "Gated form" } %}

{% endblock %}
